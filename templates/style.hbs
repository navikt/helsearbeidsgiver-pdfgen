 <style type="text/css">
    /* OpenHTMLtoPDF bruker @Page for margin */
    @page {
        margin: 0.1cm 0.5cm;
    }

    * {
        font-family: "Source Sans Pro";
    }

    p {
        margin: 0;
        padding: 0;
    }

    h3 {
        margin: 0;
        padding: 0;
        font-size: inherit;
        font-weight: inherit;
        border: none;
    }

    body {
        font-style: normal;
        font-weight: 400;
        font-size: 14px;
        margin: 1cm;
        color: #262626;
        line-height: 1.4;
    }

    .table {
        display: table;
    }

    .width-100 {
        width: 100%;
    }

    .row {
        display: table-row;
    }

    .cell {
        display: table-cell;
    }

    /* Typography Weights */
    .text-bold { font-weight: 500; }
    .text-regular { font-weight: 400; }
    .text-thin { font-weight: 200; }
    .font-semibold { font-weight: 600; }

    /* Font Sizes */
    .text-xs { font-size: 11px; }
    .text-sm { font-size: 13px; }
    .text-lg { font-size: 18px; }
    .text-xl { font-size: 20px; }

    /* Spacing */
    .mb-05 { margin-bottom: 2px; }
    .mb-1 { margin-bottom: 4px; }
    .mb-2 { margin-bottom: 8px; }
    .mb-3 { margin-bottom: 10px; }

    .bg-light-blue { background-color: #e6f0f7; }

    .rounded-md { border-radius: 8px; }

    table { width: 100%; border-collapse: collapse; }

    h1 {
        font-size: 32px;
        font-weight: 500;
        margin: 0;
        padding: 0;
    }

    h2 {
        font-size: 20px;
        font-weight: 500;
        border-top: 1px solid #e0e0e0;
        padding-top: 12px;
        margin-top: 30px;
        margin-bottom: 5px;
    }

    .hidden {
        display: none;
    }

    .main-title { font-size: 32px; font-weight: 700; }

    .metadata-text { color: #666; }

    .info-box-table {
        display: table;
        width: 100%;
        table-layout: fixed;
        border-collapse: separate;
        border-spacing: 10px 0;
        margin-left: -10px;
        margin-bottom: 20px;
        margin-top: 10px;
    }

    .info-box-row {
        display: table-row;
    }

    .info-box-cell {
        display: table-cell;
        vertical-align: top;
        width: 50%;   /* Forsikre lik bredde */
    }

    .box-patient {
        background-color: #00213d;
        color: #ffffff;
        padding: 15px 20px;
    }

    .box-employer {
        background-color: #e6f0f7;
        padding: 15px 20px;
    }

    .liten-label {
        margin-bottom: 2px;
        font-weight: 600;
        font-size: 11px;
    }

    .box-patient .liten-label { color: white; }
    .box-employer .liten-label { color: black; }


    .section-subtitle {
        margin-bottom: 10px;
    }

    .followup-item {
        padding-bottom: 15px;
    }

    .period-row {
        background-color: #eef4f9;
        padding: 6px 15px;
        margin-bottom: 12px;
        border-radius: 14px;
    }

    .period-row .period-type { width: 26%; }
    .period-row .period-dates { width: 35%; }

    .doctor-card {
        padding: 20px;
        margin-top: 10px;
    }

    .doctor-card-item {
        margin-bottom: 8px;
    }

    hr {
        margin-top: 30px;
        border: 0;
        border-top: 1px solid #e0e0e0;
    }

    .navlogo {
        position: absolute;
        top: 1cm;
        right: 1.5cm;
        width: 60px;
        height: auto;
    }
</style>